{
    "name": "dink-compiler",
    "scripts": {
        "clean": "cd DinkCompiler && dotnet clean",
        "build": "cd DinkCompiler && dotnet build",
        "build-release": "npm run clean && cd DinkCompiler && dotnet build -c Release",
        "test": "npm run build && cd DinkCompiler.Tests && dotnet test",
        "dist": "rm -rf ./dist && npm run build-release && mkdir -p ./dist && cp ./DinkCompiler/bin/Release/net8.0/DinkCompiler.dll ./dist && cp ../README.md ./dist",
        "pack": "npm run dist && mkdir -p ../dist/v$npm_package_version && rm -rf ../dist/v$npm_package_version/$npm_package_name-v$npm_package_version.zip && zip -r ../dist/v$npm_package_version/$npm_package_name-v$npm_package_version.zip ./dist"
    }
  }
  